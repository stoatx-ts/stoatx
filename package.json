{
  "name": "marshmallow-stoat-monorepo",
  "version": "1.0.0",
  "description": "Moderation bot for Stoat and documentation fot the bot",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "pnpm -r build && changeset publish",
    "format": "pnpm -r format"
  },
  "keywords": ["stoat", "moderation", "bot", "documentation", "stoatbot"],
  "homepage": "https://docs.marshmallow-stoat.com",
  "author": "ispik <ispik@ispik.dev>",
  "license": "AGPL-3.0-or-later",
  "packageManager": "pnpm@10.28.0",
  "devDependencies": {
    "@changesets/cli": "^2.29.8",
    "@changesets/changelog-github": "^0.5.0"
  },
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=10.0.0"
  }
}
